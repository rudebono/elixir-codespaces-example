{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start elixir-codespaces-example",
            "type": "shell",
            "command": "mix deps.get && mix ecto.reset && iex -S mix phx.server",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "group": "run"
            }
        }
    ]
}